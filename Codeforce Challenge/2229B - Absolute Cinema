t = int(input())
for j in range(t):
    n = int(input())
    a = list(map(int, input().split()))
    b = list(map(int, input().split()))
    ans = 0
    best = 0
    for i in range(n):
        ans += max(a[i], b[i])
        best = max(best, min(a[i], b[i]))
    print(ans + best)
